<odoo>
  <template id="template_secret" name="Demo Insecure Secret Page">
    <t t-call="web.layout">
      <div class="o_container o_main_content mt16 mb16">
        <div class="o_form_view o_view_controller">
          <div class="o_form_sheet_bg">
            <div class="o_form_sheet">
              <div class="card p-4">
                <h2 class="mb-2">Demo Insecure — Formulario</h2>
                <p class="text-muted">Formulario de ejemplo. Inspecciona el DOM para ver la credencial expuesta.</p>
                <form id="demoForm" class="o_form o_form_editable mt-3">
                  <div class="row">
                    <div class="col-12 mb-2">
                      <label class="form-label">Nombre</label>
                      <input class="form-control" type="text" value=""/>
                    </div>
                    <div class="col-12 mb-2">
                      <label class="form-label">Email</label>
                      <input class="form-control" type="email" value=""/>
                    </div>
                  </div>

                  <!-- Vulnerabilidad: credencial expuesta en attribute data-api-key y en input hidden -->
                  <div id="demo_info" t-att-data-api-key="secret" style="display:none"></div>
                  <input type="hidden" id="api_key" name="api_key" t-att-value="secret"/>

                  <div class="mt-3">
                    <button type="button" class="btn btn-primary" onclick="alert('Demo - no envía')">Enviar (demo)</button>
                  </div>
                </form>
                <p class="text-muted small mt-2">Nota: la API key está presente en el DOM pero no visible en la UI.</p>
              </div>
            </div>
          </div>
        </div>
      </div>
      <t t-javascript="true">
        console.log("Demo: API_KEY (from DOM) =", document.getElementById('api_key')?.value);
      </t>
    </t>
  </template>
</odoo>